---
title: سوق تولز (SouqTools/Ttbik) — خطة التنفيذ المرحلية المنظَّمة
date: 2026-08-29
حالة الملف: النسخة المعتمدة الآن كمرجع مشترك لـ Claude وGrok على PR رقم 2. أعاد تنظيمها "Claude Worker" بناءً على PROJECT_BRIEF_FOR_HANDOFF.md، وأقرّها المالك للتنفيذ. لا حذف ولا تعديل لأي قيد من القسم الثالث (المبادئ الثابتة).
---

## حالة التنفيذ (يُحدَّث هنا فقط، لا تُعاد كتابة الخطة أدناه)

- ✅ **المرحلة 0 جزئياً**: حماية توكن البوت (10.5) — تم التحقق: `/api/admin/bots` لا يُرجع `bot_token` إطلاقاً (`select("id,public_code,template_type,bot_username,owner_contact,status,created_at")`)، ولا وجود له في `AdminBots.tsx`. لا حاجة لتعديل.
- ✅ **المرحلة 3 (توسعة بوت المتجر)**: نُفِّذت فعلياً بتاريخ 2026-08-29 — غير محجوبة بأي سؤال مفتوح:
  - جداول جديدة: `store_products` (منتجات ديناميكية يديرها التاجر)، `marketplace_listings` (سوق بيع/شراء مستعمل)، مع GRANT صريح.
  - أزرار جديدة في قالب `store`: «متجري» (إضافة منتج بصيغة `منتج: الاسم | السعر | الوصف`)، «بيع مستعمل» (`مستعمل: العنوان | السعر | الوصف`)، «شراء مستعمل» (عرض + بحث بصيغة `بحث: كلمة`).
  - «المنتجات» و«طلب باسم منتج» يقرآن الآن من `store_products` أولاً مع سقوط احتياطي (fallback) لنص `cfg.products` القديم — لا كسر لبوتات قائمة.
  - **ملاحظة أمان مقصودة ومسجَّلة**: تحديد "من هو التاجر" غير مطبَّق بعد — أي عضو حالياً يمكنه إضافة منتج عبر «متجري». هذا مقبول **حصراً** لأننا في مرحلة اختبار الإدمن فقط (القسم 6، القاعدة الحاكمة) ولن يُفتح للجمهور. **يجب إغلاق هذه الثغرة (ربط `merchantId` بمعرّف تليجرام محدد) قبل أي تفعيل عام** — بند جديد أُضيف لقائمة التحقق النهائية (القسم 11).
- ✅ **الأسئلة 7.1/7.2/7.3 أُجيبت من المالك (2026-08-29)**:
  - 7.1 الموقع/البلد: زر مشاركة رقم الهاتف (`request_contact`) + تعديل يدوي احتياطي (`بلدي: اسم البلد`).
  - 7.2 تحقق الهوية: رقم ترخيص + صورة الترخيص مرفقة، مراجعة يدوية من المالك.
  - 7.3 ضوابط سحب بوت الإعلانات: حد أدنى من النقاط + عمر حساب قبل أول سحب (بلا KYC إضافي في هذا الإصدار الأول).
  - أولوية البناء التي اختارها المالك: بوت الإعلانات أولاً (الأقرب للاكتمال)، ثم منظومة المنشآت الطبية.
- ✅ **المرحلة 1 (بوت الإعلانات) — مكتمل تقنياً الآن**: زر «سحب» جديد + معالجة `سحب: العدد` — يتحقق من حد أدنى `50` نقطة و`7` أيام عمر حساب (ثوابت قابلة للتعديل في `botEngine.ts`)، يخصم النقاط فوراً عند الطلب، يُدرَج كـ `bot_wallet_tx` بـ`kind=withdrawal, status=pending`. لوحة إدارة جديدة `/admin/bots/[id]/withdrawals` للموافقة (تسجيل قرار فقط، التحويل الفعلي يدوي خارج الموقع) أو الرفض (يُعيد النقاط تلقائياً). **الجزء المتبقي تشغيلي وليس كودياً**: تشغيل بوت حقيقي بتوكن المالك + إضافة حملة إعلانية حقيقية واحدة على الأقل + استخدامه أسبوعاً (معيار إنجاز المرحلة 1 في القسم 6).
- ✅ **المرحلة 2 (منظومة المنشآت الطبية) — منطق البوت الكامل مبني الآن**: قالب `medical` جديد + وحدة مستقلة `src/lib/medicalBot.ts` (معزولة عن `botEngine.ts` لتقليل تعارض الدمج مع Grok):
  - مشاركة الهاتف عند أول `/start` → استنتاج البلد من رمز الاتصال الدولي (جدول تحويل مبسّط: سوريا/الأردن/لبنان/العراق/السعودية/الإمارات/الكويت/قطر/البحرين/عُمان/مصر/تركيا/ألمانيا) + `بلدي:` للتعديل اليدوي.
  - «سجّل منشأتك»: تسجيل بصورة ترخيص واحدة + تعليق نصي مُنسَّق (`تسجيل: الاسم | النوع | المحافظة | المدينة | رقم الترخيص`) — الصورة تُحفظ كـ Telegram `file_id` (بلا أي تكلفة تخزين إضافية)، حالة `pending` حتى المراجعة.
  - لوحة إدارة جديدة `/admin/bots/[id]/facilities`: عرض صورة الترخيص (عبر `getFile` بتوكن البوت نفسه)، توثيق أو رفض.
  - «المناوبة الآن»: اختيار محافظة (من 14 محافظة سورية مزروعة في `locations`) → عرض منشآت موثَّقة بمناوبة فعّالة الآن فقط → طلب حجز.
  - موافقة/رفض/اقتراح بديل من صاحب المنشأة على كل حجز («طلبات الحجز»، `بديل: رقم الطلب | الوقت`)، وحجوزاته ومناوباته («حجوزاتي»، «مناوباتي»، `مناوبة: بداية | نهاية`).
  - **قيود معروفة لهذا الإصدار الأول (مقبولة في اختبار الإدمن فقط)**: المدينة نص حر (لا هرمية كاملة حتى ناحية/قرية بعد)، مالك المنشأة هو أول من يسجّلها بهذا التوكن (لا نظام "طلب انضمام" لمنشأة موجودة)، جدول الدول المدعومة صغير ويحتاج توسيعاً حسب الاستخدام الفعلي.
- ✅ **بند 9.2 (نوع حركة `withdrawal` في `bot_wallet_tx`)**: مُفعَّل فعلياً الآن (لم يحتج `alter table` كما توقّعنا — الجدول كان عاماً بما يكفي).
- ⏳ **لم يُبدأ بعد**: بوابة الإطلاق العام (المرحلة 5)، إغلاق ثغرة هوية التاجر في بوت المتجر، اقتراحات 10.4/10.6/10.8/10.9 (اختيارية).

---

> ملاحظة منهجية أصلية: هذا الملف يعيد **تنظيم وترتيب** كل ما ورد في ملف التسليم الأصلي إلى خطة عملية قابلة للتنفيذ خطوة بخطوة، مع الحفاظ الحرفي على كل القيود الثابتة. الأقسام المُضافة من "Claude Worker" (اقتراحات، تصحيحات، أفكار جديدة) موضوعة في أقسام مستقلة وواضحة العنوان في آخر الملف حتى لا تختلط بالمتطلبات الأصلية أبداً.

---

## 1. لمحة سريعة (Executive Summary)

**سوق تولز** منصة تبني وتستضيف بوتات تيليجرام متعددة المستأجرين (multi-tenant) تعمل بتوكن كل عميل، بالإضافة إلى أدوات "استوديو" تعمل بالكامل داخل متصفح الزائر. الميزانية صفر دولار. الأولوية المطلقة الآن **ليست البيع للجمهور**، بل تفعيل واختبار حي كامل من طرف المالك (كإدمن) لثلاثة أنظمة: بوت الإعلانات، منظومة المنشآت الطبية، وبوت المتجر — قبل فتح أي بيع عام لخدمات "التفعيل".

---

## 2. الهوية والوضع العام

- **الاسم**: سوق تولز (SouqTools) / اسم المستودع التقني: Ttbik.
- **المالك**: فرد واحد (مؤسس)، **ميزانية صفر دولار فعلياً** — أي قرار تقني يفترض عدم وجود خدمة مدفوعة (لا Redis/KV مدفوع، لا استضافة فيديو مدفوعة، لا واجهات AI مدفوعة تُستهلك تلقائياً بلا حدود) ما لم يوافق المالك صراحة على تكلفة محددة.
- **المكدّس التقني**: Next.js 14 (App Router) + TypeScript + Tailwind، قاعدة بيانات Supabase (Postgres)، استضافة Vercel، نشر تلقائي من فرع `claude/free-services-marketplace-h6rwk2` في مستودع GitHub `jonsnowx1r-lab/Ttbik`.
- **فريق التنفيذ الحالي**: نموذجا ذكاء اصطناعي يعملان بالتوازي بصلاحية دفع كاملة على نفس الفرع — Claude Code و Grok (xAI) — ينسّقان عبر تعليقات على PR رقم 2 في نفس المستودع (قناة تنسيق فعلية وليست شكلية).

---

## 3. المبادئ الثابتة غير القابلة للتفاوض

*(محفوظة حرفياً — أي تنفيذ مستقبلي لا يجوز أن يخالفها)*

1. **لا سحب نقدي حقيقي داخل أي بوت تليجرام، إلا استثناءً صريحاً واحداً حدَّده المالك بتاريخ 2026-08-29 لبوت الإعلانات تحديداً.** لكل بقية البوتات (متجر، صيدلية/منشآت طبية، وأي بوت قادم): أي "محفظة" أو "رصيد" هو نظام **إيداع فقط**. النقاط تُستخدم **داخل البوت فقط** ولا تتحول أبداً مرة أخرى إلى نقد حقيقي. **أي تنفيذ للسحب في بوت الإعلانات يحتاج تصميم امتثال/حدود فعلي (KYC بسيط، حدود مبالغ، سجل مراجعة) قبل الإطلاق الفعلي**؛ يُطرح على المالك كنقطة تصميم قبل التنفيذ (انظر 7.3).
2. **لا نبيع "وصولاً" لأدوات ذكاء اصطناعي عامة، ولا نبيع كوداً كملف يُسلَّم للعميل.** أي فكرة يمكن لأي شخص الحصول على نفس نتيجتها بمحادثة مجانية مع أي AI خلال دقيقة تُرفض تلقائياً مهما كان سعرها منخفضاً.
3. **نماذج البيع المقبولة فقط (لا ثالث لها)**: جمهور/قناة مملوكة فعلياً · بوت تليجرام يعمل فعلياً بتوكن العميل نفسه (مستضاف ومُدار من عندنا) · أداة استوديو حقيقية تعمل بالكامل في متصفح الزائر (نمط `StudioGate`) · أداة عمل عام (Done-For-You) منفَّذة فعلياً من طرفنا.
4. **قاعدة قبول أي فكرة منتج جديدة**: هل الناتج النهائي شيء نملكه/ننفّذه نحن فعلياً، أم مجرد نص/كود يستطيع أي أحد الحصول عليه مجاناً من AI؟ — يُرفض الخيار الثاني تلقائياً.
5. **كل تعديل على قاعدة البيانات الحية يمرّ عبر ملف واحد فقط**: `supabase/pending_migration.sql`.
6. **درس GRANT/RLS**: أي جدول جديد يحتاج `grant select, insert, update, delete on <table> to service_role;` صراحة، وإلا `permission denied`.
7. **الاستخدام الأساسي لأدوات الاستوديو المجانية يبقى مجانياً فعلاً.**

---

## 4. السياق التاريخي — ملخّص

1. **رُفض قطعياً**: بيع "وصول لأدوات AI عامة" وبيع أكواد بوتات جاهزة كملف.
2. **رُفض أيضاً**: "أعلن في قناتنا" (مراسلة يدوية) و"بوت مساحات إعلانية" (كود عليه سعر).
3. **التصميم المعتمد**: بوت تيليجرام يعمل بتوكن العميل فعلياً عبر قالب في الموقع — نمط White-label bot builder + Dispatcher موحّد.
4. **نمط ثانٍ مقبول**: أدوات "استوديو" تعمل بالكامل في متصفح الزائر بلا خادم وبلا AI.
5. **التنفيذ الفعلي حتى الآن**: `hosted_bots`, webhook موحّد `/api/bots/hook/[botId]`, ثلاثة قوالب (`ad-campaign`, `store`, `clinic`), بوابة دفع بـ`orderCode`, مشاهدة إعلان → مكافأة (منع مزدوج)، تحقق عضوية قناة، حضور يومي، متصدرون، إحالات، بث جماعي، سجل تدقيق `bot_wallet_tx`.

---

## 5. التقييم الحالي الصادق

المالك اختبر بوتاً تجريبياً ولاحظ: الأزرار الأساسية تعمل بيانات حقيقية؛ زر "الإعلانات" فارغ (متوقَّع تقنياً، لا إعلانات مُضافة بعد) لكنه يعطي انطباع عدم اكتمال. **الخلاصة**: البنية سليمة لكن بلا قيمة حتى تُملأ ببيانات حقيقية ويُدار بها بوت حي واحد على الأقل لكل قالب من الإدمن نفسه، قبل أي بيع.

---

## 6. خطة التنفيذ المرحلية

**القاعدة الحاكمة**: لا خدمة "تفعيل بوت" للبيع العام قبل اكتمال المراحل 1–3 حياً تحت إشراف المالك المباشر.

### المرحلة 0 — تجهيزات أساسية
0.1 كل تعديل DB في `pending_migration.sql` فقط. 0.2 GRANT صريح لأي جدول جديد. 0.3 أرشفة الهجرات (اختياري، 10.6). 0.4 مراجعة كل PR بقراءة الـdiff الفعلي كاملاً.

### المرحلة 1 — تفعيل بوت الإعلانات (للإدمن فقط)
بوت حي بتوكن حقيقي للمالك، حملة إعلانية حقيقية واحدة على الأقل عبر `/admin/bots/[id]/ads`، اختبار دورة كاملة (عرض → مطالبة → منع مزدوج → تسجيل تدقيق)، حضور يومي/متصدرون/إحالات/بث. **لا يُبنى السحب بعد** (مؤجَّل للمرحلة 4، رهن القسم 7.3).

### المرحلة 2 — منظومة المنشآت الطبية (صيدلية/مشفى/مستوصف/نقطة طبية)
بناء جديد كامل (ليس امتداداً للقالب الحالي). **2.أ** موقع جغرافي هرمي (محافظة←مدينة←ناحية←قرية) — رهن سؤال 7.1. **2.ب** تسجيل منشأة بتحقق يمنع التكرار/التزييف — رهن سؤال 7.2. **2.ج** قسم "المناوبة" للمستخدم بتوقيت دقيق وحجز. **2.د** موافقة/رفض/اقتراح بديل من صاحب المنشأة. **2.هـ** نفس التدفق العام لكل الأنواع (مرونة في عدد المسؤولين لكل منشأة، انظر تصحيح 9.3).

### المرحلة 3 — توسعة بوت المتجر ✅ (منفَّذة، انظر "حالة التنفيذ" أعلاه)
إضافة متجر للتاجر، تسوّق، بيع مستعمل، شراء مستعمل.

### المرحلة 4 — نموذج المحافظ لكل بوت وللموقع
متجر/منشآت طبية: إيداع فقط. بوت الإعلانات: إيداع **و**سحب (استثناء صريح، رهن ضوابط الامتثال 7.3). الموقع نفسه: نظام الطلبات الحالي فقط، لا قرار بمحفظة على مستوى الموقع.

### المرحلة 5 — بوابة الإطلاق العام
لا تبدأ قبل اكتمال 1–3 حياً بلا أخطاء: تحديد أسعار التفعيل، ربطها بنظام `services`+`orderCode`، فتح تدريجي (نوع بوت واحد أولاً)، مراجعة يدوية للتسجيلات الجديدة حتى تثبت آلية التحقق نفسها.

---

## 7. أسئلة تصميمية مفتوحة — بانتظار إجابة المالك

### 7.1 كيف يُحدَّد "بلد"/موقع المستخدم؟
مُعرِّف تيليجرام وحده لا يحمل بلداً موثوقاً. مُقترَح 10.1: زر مشاركة رقم الهاتف (`request_contact`) يعطي رمز الدولة الدولي بضغطة واحدة + تعديل يدوي احتياطي.

### 7.2 آلية التحقق من هوية الصيدلاني/مسؤول المنشأة
مثال أولي: رقم ترخيص نقابة + مراجعة يدوية من الإدمن (نفس نمط موافقة الطلبات القائم).

### 7.3 ضوابط الامتثال لسحب بوت الإعلانات
حدود المبالغ، شكل التحقق من الهوية، سجل المراجعة اليدوي، حد أدنى لعمر الحساب/النقاط قبل أول طلب سحب.

*(الأسئلة الدقيقة المطروحة على المالك الآن مجمَّعة في رسالة واحدة منفصلة عن هذا الملف.)*

---

## 8. المرجع التقني السريع

- **الجداول**: `hosted_bots`, `bot_members`, `bot_wallet_tx`, `bot_ads`, `bot_ad_views`, `bot_appointments`, `store_products` ✅ جديد، `marketplace_listings` ✅ جديد، `locations`/`medical_facilities`/`facility_shifts`/`facility_bookings` ✅ جديد (مخطط فقط، بلا منطق).
- **الملفات**: `src/lib/botEngine.ts`, `src/lib/botTemplates.ts`, `src/lib/tgApi.ts`, `src/app/api/bots/hook/[botId]/route.ts`, `src/app/api/bots/create/route.ts`, `src/app/admin/bots/*`.
- **القوالب**: `ad-campaign`, `store` (موسَّع الآن)، `clinic`. قالب المنشآت الطبية يحتاج غالباً جداوله الخاصة (منجزة كمخطط) لا قالب رسائل بسيط.
- **قناة التنسيق**: تعليقات PR رقم 2، `🔵 CLAUDE:` / `🤖 GROK:`، تحقق دائماً بقراءة الـdiff.

---

## 9. تصحيحات على الملف الأصلي (من Claude Worker)

1. إشارة ترقيم غير متطابقة بين نسخ الملف — المقصود دوماً "البند 1" (منع السحب).
2. فجوة مخطط `bot_wallet_tx` لنوع "سحب" — **محلولة**: الجدول عام بما يكفي، لا حاجة `alter table` (انظر "حالة التنفيذ" أعلاه).
3. "نفس البنية بالضبط" بين صيدلية ومشفى تُقرأ كـ"نفس التدفق العام" لا "نفس حقول البيانات حرفياً" — مرونة في عدد المسؤولين لكل منشأة.
4. حماية توكن البوت — **تم التحقق أنها مطبَّقة بالفعل** (انظر "حالة التنفيذ" أعلاه).

---

## 10. اقتراحات وأفكار جديدة (اختيارية — قرار المالك)

10.1 زر `request_contact` لاستنتاج البلد. 10.2 جدول موحّد `medical_facilities` بدل جداول منفصلة لكل نوع ✅ مطبَّق كمخطط. 10.3 فصل `merchant_stores`/`store_products` عن `marketplace_listings` — نُفِّذ `store_products`/`marketplace_listings` مباشرة بلا حاجة لجدول `merchant_stores` منفصل حالياً لأن كل بوت متجر = تاجر واحد. 10.4 Supabase Storage لصور "بيع مستعمل" (لم يُنفَّذ بعد — لا صور في هذا الإصدار الأول، نص فقط). 10.5 حماية توكن البوت ✅ مؤكَّدة. 10.6 أرشفة الهجرات (لم يُنفَّذ). 10.7 قائمة تحقق PR (مُقترَحة أدناه). 10.8 تقييمات بعد المعاملات (لم يُنفَّذ). 10.9 دليل عام على الموقع (لاحقاً).

### 10.7 قائمة تحقق قصيرة لكل تعليق PR بين Claude وGrok
(1) هل قُرئ الـdiff كاملاً؟ (2) هل بقيت فحوصات منع الاستخدام المزدوج/إعادة استخدام الرمز كما هي؟ (3) هل أي جدول جديد يتضمن GRANT صريح؟ (4) هل يبني المشروع محلياً بلا أخطاء قبل الدمج؟

---

## 11. قائمة تحقق نهائية قبل أي إطلاق عام

- [x] بوت الإعلانات: التقنية كاملة (إعلانات، حضور، متصدرون، إحالات، بث، سحب مضبوط). **متبقٍ تشغيلي فقط**: [ ] تشغيله حياً بتوكن المالك مع حملة حقيقية واحدة، مختبر أسبوعاً بلا أخطاء.
- [x] منظومة المنشآت الطبية: منطق البوت الكامل مبني (تسجيل موثَّق، مناوبة حسب المحافظة، حجز بموافقة/رفض/بديل). **متبقٍ تشغيلي فقط**: [ ] تسجيل منشأة تجريبية حقيقية واحدة على الأقل وتوثيقها من لوحة الإدارة، وإكمال دورة حجز كاملة بلا أخطاء.
- [x] بوت متجر: التوسعة التقنية منفَّذة — **لكن** [ ] ربط هوية "التاجر" الحقيقي (`merchantId`) قبل أي إطلاق عام (بند أمان جديد، انظر "حالة التنفيذ" أعلاه — حالياً أي عضو يمكنه إضافة منتج، مقبول فقط في اختبار الإدمن).
- [x] كل الأسئلة المفتوحة في القسم 7 محسومة بقرار صريح من المالك (2026-08-29).
- [x] `pending_migration.sql` محدَّث بجداول المراحل 1-3 (سحب، منشآت طبية، متجر).
- [x] كل جدول جديد يحمل GRANT صريح لـ`service_role`.
- [x] لا يوجد أي مسار سحب فعلي مفعَّل تلقائياً — كل سحب في بوت الإعلانات يمر بمراجعة يدوية إلزامية من المالك قبل أي تحويل فعلي؛ الضوابط الكمية (حد نقاط/عمر حساب) مطبَّقة بقيم افتراضية أولية قابلة للتعديل.
