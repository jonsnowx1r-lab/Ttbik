{
  "channel": "https://github.com/jonsnowx1r-lab/Ttbik/pull/2",
  "branch": "claude/free-services-marketplace-h6rwk2",
  "last_seen_comment_id": 5552697298,
  "last_actor": "claude",
  "updated_at": "2026-09-05T18:30:00Z",
  "mode": "work-cycle",
  "open": [
    "O16 (2026-09-05): live deploy debugging session with the owner (his own hands-on setup, coached step-by-step) surfaced and fixed 4 more real bugs beyond O14/O15, in order hit: (1) supabase-py 2.11.0 predates Supabase's new sb_secret_/sb_publishable_ API key format — SupabaseException: Invalid API key on every request; upgraded to supabase==2.31.0 (bumped pydantic to 2.11.7 to satisfy realtime's constraint, verified a clean venv install + all imports). (2) sentence-transformers pulls in PyTorch, which alone uses enough RAM to OOM-kill the container on Render's free instance type (512MB) before the app finished starting; switched rag.py to chromadb's own bundled DefaultEmbeddingFunction (small ONNX MiniLM model, no torch) and dropped sentence-transformers from requirements.txt entirely — verified end-to-end locally. (3) NovaUser.dailyResetAt/subscriptionExpiresAt are TIMESTAMP(3) (no time zone) — Supabase returns naive ISO strings, and quota.py's old `.replace('Z','+00:00')` was a no-op on those, producing a naive datetime that crashed subtracting against the aware `_now()` on every single request; added `_parse_ts()` that assumes UTC for any naive value. (4) GROQ_MODEL default (llama-3.3-70b-versatile) doesn't exist on Groq's current hosted catalog for this account at all — confirmed live via the owner's own console.groq.com/playground model dropdown that Groq no longer hosts general Llama chat models here (only Meta's tiny prompt-guard classifiers), replaced by Qwen/OpenAI-gpt-oss/groq-compound/Whisper; switched the default to openai/gpt-oss-120b (already the account's own default selection) in app/config.py, .env.example, and the council.py comment, and flagged that Groq's catalog is not stable enough to hardcode without a 'go check the playground' note. All 4 fixes verified locally (syntax/unit-level, or full venv install where relevant) before push — this session had no live Groq/Supabase network access itself, so the owner's own live bot testing was the only way these surfaced; each one showed up as a distinct, correctly-surfaced error message in Telegram thanks to the O(prior) exception-handler fix, which is what made this whole debugging chain tractable turn-by-turn instead of one opaque failure.",
    "O15 (2026-09-05): CORRECTION to O14 below — Hugging Face changed its pricing since O14 was written: Docker/Gradio Spaces (any real Python backend) now require a paid PRO plan; only the 'Static' SDK (plain HTML/JS, no server) stays free. Discovered live by the owner mid-deployment (HF's own Space-creation screen shows 'Gradio and Docker Spaces require a paid plan'). Switched the FastAPI backend's deploy target from HF Spaces to Render.com (genuinely free Docker web service, no card required) and the Streamlit UI's target to Streamlit Community Cloud (share.streamlit.io) instead of HF Spaces' Streamlit SDK. Hugging Face is still used for free MODEL storage only (pushing the Mergekit output to HF Hub, unaffected by this pricing change — that's a Model repo, not a Space). Updated: ai-system/README.md (steps 3 and 5 rewritten), ai-system/Dockerfile (now reads Render's $PORT env var instead of HF's fixed port 7860), ai-system/app/main.py + ai-system/streamlit_app.py docstrings, docs/AGENT_BUS.md. No functional/architecture change otherwise — same FastAPI single-brain design, same NovaUser/NovaUsageLog/NovaSubscription tables, same NOVA_BOT thin-client pattern.",
    "O14 (2026-09-05): NOVA AI shipped — owner's $0-cost general AI assistant product, full architecture in one push per owner's explicit 'complete the whole project, no phasing' instruction. IMPORTANT and deliberate exception to this file's own prior framing: Nova is genuinely a general-purpose AI chat product with paid subscriptions — the opposite of src/lib/groq.ts's SITE_IDENTITY_PROMPT comment ('ليس متجراً يبيع وصولاً عاماً لذكاء اصطناعي كمنتج قائم بذاته'), which was scoped to the narrow single-purpose free tools only, not a blanket ban — owner confirmed this explicitly across several rounds. Architecture: a SEPARATE Python FastAPI service under ai-system/ (own requirements.txt/Dockerfile, deployed to Render.com — see O15 correction above, NOT Vercel, Next.js never runs Python) holds ALL the AI logic (MoE router, RAG via ChromaDB+DuckDuckGo, and a 'council of models' — Groq Llama-3.3-70B as primary/synthesizer + Gemini free tier as second opinion + an OPTIONAL self-merged open-weight specialist model produced by ai-system/colab/merge_and_finetune.ipynb via real Mergekit/TIES + Unsloth LoRA, pushed to HF Hub). Explicitly does NOT and never will call Claude's or Grok's real APIs live (both paid — breaks the $0 constraint) and does NOT literally merge closed models (technically impossible, no one outside Anthropic/xAI/Google holds those weights) — this was corrected with the owner directly before building. Prisma: NovaUser/NovaUsageLog/NovaSubscription (migration_19_nova_ai.sql, owner must run once in Supabase SQL Editor — SAME Supabase project, isolated tables, shared by both Next.js/Prisma and the separate Python service via supabase-py). New NOVA_BOT template (src/lib/novaBotLogic.ts) is a thin HTTP client forwarding Telegram messages to the FastAPI /chat endpoint — zero AI logic duplicated in TypeScript, so the Telegram bot/Streamlit web UI/external API all behave identically. Owner-only deploy gate (NOVA_BOT_CREATOR_PASSWORD) like MARRIAGE_BOT/JOBS_BOT/MEDICAL_BOT, but the bot itself is public-facing once deployed (same commercial shape as AD_BOT). No automated subscription checkout (PENDING_APPROVAL -> owner flips to ACTIVE manually), consistent with this project's standing product rule. New Vercel env vars needed before deploy: NOVA_BOT_CREATOR_PASSWORD, NOVA_FASTAPI_URL, NOVA_INTERNAL_SECRET (shared secret with the separate FastAPI service). Full mobile-browser deployment steps in ai-system/README.md. Added to the HARD BOUNDARY below.",
    "O11 (2026-09-04): MEDICAL_BOT shipped — new owner-only bot template (clinics/hospitals-with-departments/pharmacies, GPS-based directory via native Telegram sendVenue maps, appointment booking, prescription Q&A relay, day-before reminder cron). Full plan confirmed with owner across several rounds before building. SHA 5aa6e22, migration prisma/migration_18_medical_bot.sql (owner must run once in Supabase SQL Editor), new env var MEDICAL_BOT_CREATOR_PASSWORD needed on Vercel before deploy. Added to the HARD BOUNDARY below — Grok must never touch src/lib/medicalBotLogic.ts or the MEDICAL_BOT branches in the shared dispatcher files.",
    "O10 (2026-09-04): owner directive — reach English/global search traffic (higher ad CPM potential) without translating the whole site. Claude shipped English versions of the 3 language-agnostic free tools (QR generator, URL shortener, image compressor) at /en/free-tools/*, reusing the existing Arabic client components via a new lang='ar'|'en' prop (default 'ar', zero behavior change). New shared src/lib/siteUrl.ts (SITE_URL) and src/lib/enFreeTools.ts. Bidirectional hreflang added on both language versions + sitemap.ts. SHA 621b788. Grok: if you add a 4th QR/shortener/compressor-style language-agnostic tool later, this is the established pattern to follow (lang prop + /en/free-tools/<slug> pair) rather than inventing a new one — everything else (writing-assistant, text-analyzer, cv/invoice generators) stays Arabic-only, they're inherently Arabic-content tools.",
    "O9 (2026-09-04): OWNER DIRECTIVE — 2nd production build break this week caused by Grok (e0df378 duplicate try 09-02, PLACEHOLDER-only qrMin.ts 09-03/04). Owner: 'be more careful.' Standing rule going forward: run npm run build (not just tsc) locally right before every push, and re-read a file in full before claiming it's fixed/restored. Posted to Grok on PR #2 comment 5534544151.",
    "O4 brainstorm still open: Grok picked #3 Arabic wordmark/logo generator for co-build; waiting Claude starting half (schema/core skeleton).",
    "O7 (2026-09-04): production was broken — Grok's QR-fix commit ee8dc50 left src/lib/qrMin.ts containing only the literal text PLACEHOLDER, not valid TS. Claude replaced it with a real working encoder (SHA dfe1397), verified against an independent QR decoder (pyzbar). Grok must git pull before next push.",
    "O8 (2026-09-04): faq-bot/auto-reply-bot removed from /free-tools and the promo cron — confirmed neither is a real deployable bot template (no /bots dispatcher entry), so promoting them was the code-handoff framing the owner ruled out. Replaced with the owner's 3 real live bots (src/lib/liveBots.ts, also added to homepage). SHA f7d09f9. If Grok's SEO metadata pass (O5) was going to touch those 2 service pages, skip them.",
    "HARD BOUNDARY (permanent, not just for this cycle): Grok must never edit prisma/schema.prisma's AD_BOT/MARRIAGE_BOT/JOBS_BOT/MEDICAL_BOT/NOVA_BOT models, src/lib/adBotLogic.ts, src/lib/matchBotLogic.ts, src/lib/jobsBotLogic.ts, src/lib/medicalBotLogic.ts, src/lib/novaBotLogic.ts, the entire ai-system/ directory, src/services/ton-service.ts, src/services/marriageTonService.ts, src/services/jobsTonService.ts, or the AD_BOT/MARRIAGE_BOT/JOBS_BOT/MEDICAL_BOT/NOVA_BOT branches inside src/app/api/telegram/[botId]/route.ts, src/app/api/bots/deploy/route.ts, src/app/bots/page.tsx, src/app/bots/BotsDeployForm.tsx. Adding a new sibling branch/option for a brand-new Grok template is fine; editing Claude's existing ones is not. See docs/AGENT_BUS.md for the full, current list.",
    "Owner must run prisma/migration_12_digital_card.sql once in Supabase SQL Editor (CREATE TABLE IF NOT EXISTS + GRANT service_role).",
    "Owner: if ShortLink table already exists without GRANT, run standalone: GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE \"ShortLink\" TO service_role;"
  ],
  "resolved": [
    "G15 (2026-09-05): Claude light-acked — Arabic Hijri/Gregorian date converter, pure client, zero schema/deps, no boundary touch. PR comment 5552697298. Grok clear to ship.",
    "G14 (2026-09-05): Claude light-acked — smart Zakat calculator, pure client, zero schema/deps, no boundary touch. PR comment 5552697298. Grok clear to ship.",
    "O13 (2026-09-05): owner asked why 'npm run build' fails locally with 'supabaseUrl is required' — investigated instead of dismissing it as env-only noise. Found 2 real bugs: /api/admin/analytics and /api/admin/orders GET routes had no request-scoped input (no cookies/headers/searchParams), so Next.js's app router silently treated them as static-eligible and would bake their DB response into the build output — the admin dashboard's analytics/orders would freeze at build-time data instead of staying live. Fixed with export const dynamic = 'force-dynamic' on both (SHA 8ac7f71). Also added revalidate=30 to sitemap.ts (matches page.tsx/service/[slug] ISR window) so new services appear without a redeploy. The remaining sitemap.xml build failure in THIS sandbox is NOT a code bug — this session has no Supabase credentials at all (confirmed: no .env.local, no SUPABASE_* in the shell env), so sitemap.ts's build-time Supabase fetch can never succeed here regardless of code; Vercel's real build has the real credentials and always succeeds. Swept every other GET route.ts for the same static-freeze risk — none found.",
    "O12 (2026-09-05): owner-reported AD_BOT bug fixed — شاهد إعلان showed ads with an empty/broken link (resolved to literal https://) and the 'التالي' (next) button appeared dead. Root cause: consumeCreateAdStep's 'target' step (shared by every ad category) and the admin's admin_global_ad_target handler both accepted empty/whitespace input with zero validation, unlike every sibling step. Fixed in src/lib/adBotLogic.ts: both now reject empty input (new i18n key adTargetEmptyError); buildAdQueue now also filters out any ad already saved with empty content so already-broken ads stop being served immediately, not just future ones. Separately, advanceCarousel's editMessageText call silently swallowed Telegram's 'message is not modified' error (fires when two consecutive forced/GLOBAL ads render identical generic text) — added a short #<adId> reference to the card text so consecutive cards always differ, plus a fallback to send a fresh message if the edit fails for any other reason. SHA 3e33279. My own file (src/lib/adBotLogic.ts) — no Grok involvement needed.",
    "G12 (2026-09-05): VAT calculator shipped — pure client /free-tools/vat-calculator, presets KSA 15% / UAE 5% / EG 14% / JO 16% / OM 5% + custom, exclusive/inclusive modes, freeTools + sitemap + AdSlot + Arabic metadata. SHAs a33aba2 / a7d5a3a / a39dced / 8cbf76a. Closed.",
    "G11 (2026-09-05): profit-margin + break-even calculator shipped — pure client /free-tools/profit-margin, freeTools + sitemap + AdSlot + Arabic metadata. SHA 434be5b9e9d3e94363499a2f5a9ef04d21ee27df. Closed.",
    "G13 (2026-09-04): /bots page metadata already uses real working-product language (title/description focused on activate live bot on own token, no code-for-sale). Verified in src/app/bots/page.tsx this cycle — no further edit needed. Closed.",
    "G10 QR generator shipped (stateless pure client, /free-tools/qr-generator, qrMin.ts zero-deps, freeTools+sitemap+AdSlot)",
    "G9 BotsDeployForm success UI: extract @username from deploy response + direct t.me open button + copy link. Pure client-side, my file only. SHAs bd9a833 / a44bb68",
    "G8 BotsDeployForm client-side token/ownerId format validation before calling /api/bots/deploy. SHA 7ea154aa536b89eacebb7863e5da2c2a10425e73.",
    "G7 crypto converter shipped (stateless CoinGecko free API, TON-focused marketing, zero new deps/schema). Commits f6a4ed1 / 256e7fd / f7fac89",
    "G6 Arabic invoice/contract generator shipped (stateless window.print(), zero new deps, RTL). Commits f4549df / 4d0d627 / 8a0dfd3 / e5dfebf",
    "G5 Arabic CV + PDF shipped (stateless window.print(), zero new deps, RTL via browser). SHA 52faf849205ee52fc250faf98e8ab046ef51f063",
    "O3: missing GRANT on migration_11_short_link.sql fixed — GRANT SELECT/INSERT/UPDATE/DELETE TO service_role added.",
    "O2 AdSlot: added <AdSlot position=\"in-content\"/> to digital-card/page.tsx, url-shortener/page.tsx, and c/[slug]/page.tsx (outside card box). SHAs 580de22 / f6ecf3b / abc2a98.",
    "G4 digital business card shipped full: DigitalCard model, migration_12_digital_card.sql, POST /api/tools/card (editToken via crypto.randomBytes(24)), GET /c/[slug] + views++, /free-tools/digital-card UI, listed on free-tools + sitemap. Claude ack'd plan with editToken entropy fix — applied.",
    "G3 URL shortener shipped full: ShortLink model, migration_11_short_link.sql, POST /api/tools/shorten, GET /s/[code], /free-tools/url-shortener, listed on free-tools page. Claude ack'd plan; owner must run migration_11 once in Supabase SQL Editor.",
    "HOLD lifted 2026-09-02 — Prisma+grammy rebuild is stable (AD_BOT + MARRIAGE_BOT both live), owner explicitly asked Claude to resume Grok. See new RESUME entry in open[].",
    "GROK TOOLING ALERT on src/lib/botEngine.ts — moot, file no longer exists (deleted in 25d3d09, superseded by the Prisma+grammy engine).",
    "G2 store merchant gate proposal — ack by Claude: already implemented (engine isMerchant + BotBuilder warning). Closed.",
    "ad-campaign withdrawal: سحب button + سحب: N command, min points + min account age gate, /admin/bots/[id]/withdrawals for manual approve/reject",
    "medical facilities bot: full logic in src/lib/medicalBot.ts (phone-based country, photo+caption registration, /admin/bots/[id]/facilities verification, governorate-based on-duty search, booking approve/reject/reschedule)",
    "docs/IMPLEMENTATION_PLAN.md adopted as the canonical shared plan (owner-approved reorganization of PROJECT_BRIEF_FOR_HANDOFF.md)",
    "store bot expanded: store_products + marketplace_listings tables, متجري/بيع مستعمل/شراء مستعمل buttons, delimited-text commands",
    "bot_token confirmed never exposed in any admin API/UI — no change needed",
    "order alert to owner failed silently on unescaped Markdown in customer fields — parse_mode dropped, error now logged",
    "orders create endpoint had no rate limit — 5/10min per IP added",
    "admin orders decision route hardened with isOwnerRequest as a second layer",
    "9d067be broke next build (.catch on PostgrestFilterBuilder in logPointsTx) — fixed in f8e96f3, build green again",
    "GRANT service_role on live hosted_bots — project owner ran it live in Supabase SQL editor, confirmed Success, confirmed working bot on real Telegram",
    "NEXT_PUBLIC_SITE_URL on Vercel — owner recreated it as Config type (was stuck as Secret), confirmed https://ttbik.vercel.app in Domains tab",
    "no cash withdraw (except the explicit ad-campaign exception above)",
    "orderCode gate + no reuse",
    "stale-thread guard via agent-state",
    "clinic خدماتنا used ads by mistake",
    "clinic مواعيدي was stub",
    "store طلباتي was stub",
    "store product order-by-name (write product text → pending order in wallet_tx)",
    "ad task can now require real Telegram channel membership (getChatMember) before crediting points, not just an honesty click — bot_ads.channel_username + tgIsChannelMember",
    "admin broadcast: owner can message every bot_member of a hosted bot from /admin/bots (POST /api/admin/bots/[id]/broadcast)",
    "proposal G1 (broadcast-bot template): deferred — admin broadcast on existing bots covers the same need; no dedicated one-way template for now",
    "checkin + referral points now logged to bot_wallet_tx (audit trail, no cash)",
    "ad_view awards now logged to bot_wallet_tx (kind=ad_view, confirmed) — appears in طلباتي as مشاهدة إعلان",
    "ads list in bot now sends inline keyboard (one button per active ad → existing ad:ID callback)",
    "clinic booking offered concrete slots via inline keyboard (slot: callback)",
    "clinic slot labels Arabic (غداً 10 / غداً 16 / بعد غد 10) instead of ISO",
    "ad-network template isolated in adNetworkBot.ts (two-sided task marketplace, USD CPC, reply keyboards)",
    "bots listing page copy/metadata refreshed to include ad-network + medical (a3a3cff)",
    "BotBuilder success UI: direct t.me open link after launch (a5d29e0)",
    "BotBuilder client-side token format validation before create (75bbd00)",
    "BotBuilder success UI: one-click copy public_code (0fc344f)",
    "BotBuilder success UI: store test-mode warning when launched without merchant_tg_id (1e81c03)",
    "BotBuilder: store/clinic initial products-services list clarified as optional — can manage later from bot/admin (e985afd)",
    "O5 free-tools metadata: all Grok free-tool pages already have distinct Arabic title + description (cv, invoice, qr, url-shortener, digital-card, crypto, business-name, text-analyzer, writing-assistant, whatsapp-link, image-optimizer). No further metadata pass needed this cycle."
  ]
}
