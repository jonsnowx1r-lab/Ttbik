{
  "channel": "https://github.com/jonsnowx1r-lab/Ttbik/pull/2",
  "branch": "claude/free-services-marketplace-h6rwk2",
  "last_seen_comment_id": 5488330450,
  "last_actor": "grok",
  "updated_at": "2026-09-01T06:16:00Z",
  "mode": "work-cycle",
  "open": [
    "HOLD: Claude ordered full stop after Prisma+grammy rebuild (25d3d09). Grok must not push code until explicit resume notice.",
    "GROK TOOLING ALERT (active): Grok must not edit src/lib/botEngine.ts until a confirmed clean write test. Previous attempts produced empty/PLACEHOLDER content. Engine ownership currently with Claude (rebuild). Old bot paths deleted in 25d3d09."
  ],
  "resolved": [
    "G2 store merchant gate proposal — ack by Claude: already implemented (engine isMerchant + BotBuilder warning). Closed.",
    "ad-campaign withdrawal: سحب button + سحب: N command, min points + min account age gate, /admin/bots/[id]/withdrawals for manual approve/reject",
    "medical facilities bot: full logic in src/lib/medicalBot.ts (phone-based country, photo+caption registration, /admin/bots/[id]/facilities verification, governorate-based on-duty search, booking approve/reject/reschedule)",
    "docs/IMPLEMENTATION_PLAN.md adopted as the canonical shared plan (owner-approved reorganization of PROJECT_BRIEF_FOR_HANDOFF.md)",
    "store bot expanded: store_products + marketplace_listings tables, متجري/بيع مستعمل/شراء مستعمل buttons, delimited-text commands",
    "bot_token confirmed never exposed in any admin API/UI — no change needed",
    "order alert to owner failed silently on unescaped Markdown in customer fields — parse_mode dropped, error now logged",
    "orders create endpoint had no rate limit — 5/10min per IP added",
    "admin orders decision route hardened with isOwnerRequest as a second layer",
    "9d067be broke next build (.catch on PostgrestFilterBuilder in logPointsTx) — fixed in f8e96f3, build green again",
    "GRANT service_role on live hosted_bots — project owner ran it live in Supabase SQL editor, confirmed Success, confirmed working bot on real Telegram",
    "NEXT_PUBLIC_SITE_URL on Vercel — owner recreated it as Config type (was stuck as Secret), confirmed https://ttbik.vercel.app in Domains tab",
    "no cash withdraw (except the explicit ad-campaign exception above)",
    "orderCode gate + no reuse",
    "stale-thread guard via agent-state",
    "clinic خدماتنا used ads by mistake",
    "clinic مواعيدي was stub",
    "store طلباتي was stub",
    "store product order-by-name (write product text → pending order in wallet_tx)",
    "ad task can now require real Telegram channel membership (getChatMember) before crediting points, not just an honesty click — bot_ads.channel_username + tgIsChannelMember",
    "admin broadcast: owner can message every bot_member of a hosted bot from /admin/bots (POST /api/admin/bots/[id]/broadcast)",
    "proposal G1 (broadcast-bot template): deferred — admin broadcast on existing bots covers the same need; no dedicated one-way template for now",
    "checkin + referral points now logged to bot_wallet_tx (audit trail, no cash)",
    "ad_view awards now logged to bot_wallet_tx (kind=ad_view, confirmed) — appears in طلباتي as مشاهدة إعلان",
    "ads list in bot now sends inline keyboard (one button per active ad → existing ad:ID callback)",
    "clinic booking offered concrete slots via inline keyboard (slot: callback)",
    "clinic slot labels Arabic (غداً 10 / غداً 16 / بعد غد 10) instead of ISO",
    "ad-network template isolated in adNetworkBot.ts (two-sided task marketplace, USD CPC, reply keyboards)",
    "bots listing page copy/metadata refreshed to include ad-network + medical (a3a3cff)",
    "BotBuilder success UI: direct t.me open link after launch (a5d29e0)",
    "BotBuilder client-side token format validation before create (75bbd00)",
    "BotBuilder success UI: one-click copy public_code (0fc344f)",
    "BotBuilder success UI: store test-mode warning when launched without merchant_tg_id (1e81c03)",
    "BotBuilder: store/clinic initial products-services list clarified as optional — can manage later from bot/admin (e985afd)"
  ]
}
