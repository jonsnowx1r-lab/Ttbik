{
  "channel": "https://github.com/jonsnowx1r-lab/Ttbik/pull/2",
  "branch": "claude/free-services-marketplace-h6rwk2",
  "last_seen_comment_id": 5536515027,
  "last_actor": "grok",
  "updated_at": "2026-09-04T06:16:00Z",
  "mode": "work-cycle",
  "open": [
    "O11 (2026-09-04): MEDICAL_BOT shipped — new owner-only bot template (clinics/hospitals-with-departments/pharmacies, GPS-based directory via native Telegram sendVenue maps, appointment booking, prescription Q&A relay, day-before reminder cron). Full plan confirmed with owner across several rounds before building. SHA 5aa6e22, migration prisma/migration_18_medical_bot.sql (owner must run once in Supabase SQL Editor), new env var MEDICAL_BOT_CREATOR_PASSWORD needed on Vercel before deploy. Added to the HARD BOUNDARY below — Grok must never touch src/lib/medicalBotLogic.ts or the MEDICAL_BOT branches in the shared dispatcher files.",
    "O10 (2026-09-04): owner directive — reach English/global search traffic (higher ad CPM potential) without translating the whole site. Claude shipped English versions of the 3 language-agnostic free tools (QR generator, URL shortener, image compressor) at /en/free-tools/*, reusing the existing Arabic client components via a new lang='ar'|'en' prop (default 'ar', zero behavior change). New shared src/lib/siteUrl.ts (SITE_URL) and src/lib/enFreeTools.ts. Bidirectional hreflang added on both language versions + sitemap.ts. SHA 621b788. Grok: if you add a 4th QR/shortener/compressor-style language-agnostic tool later, this is the established pattern to follow (lang prop + /en/free-tools/<slug> pair) rather than inventing a new one — everything else (writing-assistant, text-analyzer, cv/invoice generators) stays Arabic-only, they're inherently Arabic-content tools.",
    "O9 (2026-09-04): OWNER DIRECTIVE — 2nd production build break this week caused by Grok (e0df378 duplicate try 09-02, PLACEHOLDER-only qrMin.ts 09-03/04). Owner: 'be more careful.' Standing rule going forward: run npm run build (not just tsc) locally right before every push, and re-read a file in full before claiming it's fixed/restored. Posted to Grok on PR #2 comment 5534544151.",
    "O4 brainstorm still open: Grok picked #3 Arabic wordmark/logo generator for co-build; waiting Claude starting half (schema/core skeleton).",
    "O7 (2026-09-04): production was broken — Grok's QR-fix commit ee8dc50 left src/lib/qrMin.ts containing only the literal text PLACEHOLDER, not valid TS. Claude replaced it with a real working encoder (SHA dfe1397), verified against an independent QR decoder (pyzbar). Grok must git pull before next push.",
    "O8 (2026-09-04): faq-bot/auto-reply-bot removed from /free-tools and the promo cron — confirmed neither is a real deployable bot template (no /bots dispatcher entry), so promoting them was the code-handoff framing the owner ruled out. Replaced with the owner's 3 real live bots (src/lib/liveBots.ts, also added to homepage). SHA f7d09f9. If Grok's SEO metadata pass (O5) was going to touch those 2 service pages, skip them.",
    "HARD BOUNDARY (permanent, not just for this cycle): Grok must never edit prisma/schema.prisma's AD_BOT/MARRIAGE_BOT/JOBS_BOT/MEDICAL_BOT models, src/lib/adBotLogic.ts, src/lib/matchBotLogic.ts, src/lib/jobsBotLogic.ts, src/lib/medicalBotLogic.ts, src/services/ton-service.ts, src/services/marriageTonService.ts, src/services/jobsTonService.ts, or the AD_BOT/MARRIAGE_BOT/JOBS_BOT/MEDICAL_BOT branches inside src/app/api/telegram/[botId]/route.ts, src/app/api/bots/deploy/route.ts, src/app/bots/page.tsx, src/app/bots/BotsDeployForm.tsx. Adding a new sibling branch/option for a brand-new Grok template is fine; editing Claude's existing ones is not. See docs/AGENT_BUS.md for the full, current list.",
    "Owner must run prisma/migration_12_digital_card.sql once in Supabase SQL Editor (CREATE TABLE IF NOT EXISTS + GRANT service_role).",
    "Owner: if ShortLink table already exists without GRANT, run standalone: GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE \"ShortLink\" TO service_role;",
    "G11 (2026-09-04): Grok proposal — حاسبة هامش الربح ونقطة التعادل (profit margin + break-even calculator). Stateless pure client, /free-tools/profit-margin, zero schema/deps. Waiting light ack. Separate from O4 logo co-build."
  ],
  "resolved": [
    "G10 QR generator shipped (stateless pure client, /free-tools/qr-generator, qrMin.ts zero-deps, freeTools+sitemap+AdSlot)",
    "G9 BotsDeployForm success UI: extract @username from deploy response + direct t.me open button + copy link. Pure client-side, my file only. SHAs bd9a833 / a44bb68",
    "G8 BotsDeployForm client-side token/ownerId format validation before calling /api/bots/deploy. SHA 7ea154aa536b89eacebb7863e5da2c2a10425e73.",
    "G7 crypto converter shipped (stateless CoinGecko free API, TON-focused marketing, zero new deps/schema). Commits f6a4ed1 / 256e7fd / f7fac89",
    "G6 Arabic invoice/contract generator shipped (stateless window.print(), zero new deps, RTL). Commits f4549df / 4d0d627 / 8a0dfd3 / e5dfebf",
    "G5 Arabic CV + PDF shipped (stateless window.print(), zero new deps, RTL via browser). SHA 52faf849205ee52fc250faf98e8ab046ef51f063",
    "O3: missing GRANT on migration_11_short_link.sql fixed — GRANT SELECT/INSERT/UPDATE/DELETE TO service_role added.",
    "O2 AdSlot: added <AdSlot position=\"in-content\"/> to digital-card/page.tsx, url-shortener/page.tsx, and c/[slug]/page.tsx (outside card box). SHAs 580de22 / f6ecf3b / abc2a98.",
    "G4 digital business card shipped full: DigitalCard model, migration_12_digital_card.sql, POST /api/tools/card (editToken via crypto.randomBytes(24)), GET /c/[slug] + views++, /free-tools/digital-card UI, listed on free-tools + sitemap. Claude ack'd plan with editToken entropy fix — applied.",
    "G3 URL shortener shipped full: ShortLink model, migration_11_short_link.sql, POST /api/tools/shorten, GET /s/[code], /free-tools/url-shortener, listed on free-tools page. Claude ack'd plan; owner must run migration_11 once in Supabase SQL Editor.",
    "HOLD lifted 2026-09-02 — Prisma+grammy rebuild is stable (AD_BOT + MARRIAGE_BOT both live), owner explicitly asked Claude to resume Grok. See new RESUME entry in open[].",
    "GROK TOOLING ALERT on src/lib/botEngine.ts — moot, file no longer exists (deleted in 25d3d09, superseded by the Prisma+grammy engine).",
    "G2 store merchant gate proposal — ack by Claude: already implemented (engine isMerchant + BotBuilder warning). Closed.",
    "ad-campaign withdrawal: سحب button + سحب: N command, min points + min account age gate, /admin/bots/[id]/withdrawals for manual approve/reject",
    "medical facilities bot: full logic in src/lib/medicalBot.ts (phone-based country, photo+caption registration, /admin/bots/[id]/facilities verification, governorate-based on-duty search, booking approve/reject/reschedule)",
    "docs/IMPLEMENTATION_PLAN.md adopted as the canonical shared plan (owner-approved reorganization of PROJECT_BRIEF_FOR_HANDOFF.md)",
    "store bot expanded: store_products + marketplace_listings tables, متجري/بيع مستعمل/شراء مستعمل buttons, delimited-text commands",
    "bot_token confirmed never exposed in any admin API/UI — no change needed",
    "order alert to owner failed silently on unescaped Markdown in customer fields — parse_mode dropped, error now logged",
    "orders create endpoint had no rate limit — 5/10min per IP added",
    "admin orders decision route hardened with isOwnerRequest as a second layer",
    "9d067be broke next build (.catch on PostgrestFilterBuilder in logPointsTx) — fixed in f8e96f3, build green again",
    "GRANT service_role on live hosted_bots — project owner ran it live in Supabase SQL editor, confirmed Success, confirmed working bot on real Telegram",
    "NEXT_PUBLIC_SITE_URL on Vercel — owner recreated it as Config type (was stuck as Secret), confirmed https://ttbik.vercel.app in Domains tab",
    "no cash withdraw (except the explicit ad-campaign exception above)",
    "orderCode gate + no reuse",
    "stale-thread guard via agent-state",
    "clinic خدماتنا used ads by mistake",
    "clinic مواعيدي was stub",
    "store طلباتي was stub",
    "store product order-by-name (write product text → pending order in wallet_tx)",
    "ad task can now require real Telegram channel membership (getChatMember) before crediting points, not just an honesty click — bot_ads.channel_username + tgIsChannelMember",
    "admin broadcast: owner can message every bot_member of a hosted bot from /admin/bots (POST /api/admin/bots/[id]/broadcast)",
    "proposal G1 (broadcast-bot template): deferred — admin broadcast on existing bots covers the same need; no dedicated one-way template for now",
    "checkin + referral points now logged to bot_wallet_tx (audit trail, no cash)",
    "ad_view awards now logged to bot_wallet_tx (kind=ad_view, confirmed) — appears in طلباتي as مشاهدة إعلان",
    "ads list in bot now sends inline keyboard (one button per active ad → existing ad:ID callback)",
    "clinic booking offered concrete slots via inline keyboard (slot: callback)",
    "clinic slot labels Arabic (غداً 10 / غداً 16 / بعد غد 10) instead of ISO",
    "ad-network template isolated in adNetworkBot.ts (two-sided task marketplace, USD CPC, reply keyboards)",
    "bots listing page copy/metadata refreshed to include ad-network + medical (a3a3cff)",
    "BotBuilder success UI: direct t.me open link after launch (a5d29e0)",
    "BotBuilder client-side token format validation before create (75bbd00)",
    "BotBuilder success UI: one-click copy public_code (0fc344f)",
    "BotBuilder success UI: store test-mode warning when launched without merchant_tg_id (1e81c03)",
    "BotBuilder: store/clinic initial products-services list clarified as optional — can manage later from bot/admin (e985afd)",
    "O5 free-tools metadata: all Grok free-tool pages already have distinct Arabic title + description (cv, invoice, qr, url-shortener, digital-card, crypto, business-name, text-analyzer, writing-assistant, whatsapp-link, image-optimizer). No further metadata pass needed this cycle."
  ]
}
